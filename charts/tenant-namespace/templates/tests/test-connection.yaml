apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tenant-namespace.fullname" . }}-test-connection"
  labels:
    {{- include "tenant-namespace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tenant-namespace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
